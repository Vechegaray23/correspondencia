# netlify.toml
[build]
  base    = "client"                # entra a client/
  command = "npm ci && npm run build"
  publish = "dist"                  # publica client/dist (no client/client/dist)

[build.environment]
  # (opcional) añade aquí VITE_API_URL si lo necesitas